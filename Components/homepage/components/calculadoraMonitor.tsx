import { useContext } from "react";
import { DatosContext } from "./datosContext";
import { View } from "react-native";

export default function CalculadoraMonitor(){
    const{ninnos} = useContext(DatosContext);
    return(
        <View>
            
        </View>
    );
}
